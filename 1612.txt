#include<iostream>
using namespace std;
#define MAX_N 20+1
int a[MAX_N];
int n,k;
bool dfs(int i, int sum)
{
	//??????n????????????????????sum??????k????
	if(i == n)
	return sum==k;
	//??????a[i]??????
	if(dfs(i+1,sum))
	return true;
	//????a[i]??????
	if(dfs(i+1,sum+a[i]))
	return true;
	//????????????a[i]??????????k????false
    return false;
}
int main()
{
	int i;
	cin>>n;
	for(i=0;i<n;i++)
	cin>>a[i];
	cin>>k;
	if(dfs(0,0))
	cout<<"Yes"<<endl;
	else
	cout<<"No"<<endl;
	return 0; 
	
 } 